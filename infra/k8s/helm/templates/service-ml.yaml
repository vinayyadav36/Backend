apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.mlAgent.name }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.mlAgent.name }}
  ports:
    - port: {{ .Values.mlAgent.port }}
      targetPort: {{ .Values.mlAgent.port }}
      protocol: TCP
