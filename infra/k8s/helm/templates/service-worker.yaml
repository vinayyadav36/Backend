apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.worker.name }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.worker.name }}
  ports:
    - port: {{ .Values.worker.port }}
      targetPort: {{ .Values.worker.port }}
      protocol: TCP
